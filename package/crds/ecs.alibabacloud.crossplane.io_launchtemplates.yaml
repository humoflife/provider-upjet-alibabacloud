---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: launchtemplates.ecs.alibabacloud.crossplane.io
spec:
  group: ecs.alibabacloud.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - alicloud
    kind: LaunchTemplate
    listKind: LaunchTemplateList
    plural: launchtemplates
    singular: launchtemplate
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LaunchTemplate is the Schema for the LaunchTemplates API. Provides
          an ECS Launch Template resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LaunchTemplateSpec defines the desired state of LaunchTemplate
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  autoReleaseTime:
                    description: Instance auto release time. The time is presented
                      using the ISO8601 standard and in UTC time. The format is  YYYY-MM-DDTHH:MM:SSZ.
                    type: string
                  autoRenew:
                    type: boolean
                  autoRenewPeriod:
                    type: number
                  dataDisks:
                    description: The list of data disks created with instance.
                    items:
                      properties:
                        category:
                          description: 'The category of the disk:'
                          type: string
                        deleteWithInstance:
                          description: Delete this data disk when the instance is
                            destroyed. It only works on cloud, cloud_efficiency, cloud_ssd
                            and cloud_essd disk. If the category of this data disk
                            was ephemeral_ssd, please don't set this param.
                          type: boolean
                        description:
                          description: Description of instance launch template version
                            1. It can be [2, 256] characters in length. It cannot
                            start with "http://" or "https://". The default value
                            is null.
                          type: string
                        device:
                          type: string
                        encrypted:
                          description: Encrypted the data in this disk.
                          type: boolean
                        name:
                          description: Instance launch template name. Can contain
                            [2, 128] characters in length. It must start with an English
                            letter or Chinese, can contain numbers, periods (.), colons
                            (:), underscores (_), and hyphens (-). It cannot start
                            with "http://" or "https://".
                          type: string
                        performanceLevel:
                          type: string
                        size:
                          description: The size of the data disk.
                          type: number
                        snapshotId:
                          description: The snapshot ID used to initialize the data
                            disk. If the size specified by snapshot is greater that
                            the size of the disk, use the size specified by snapshot
                            as the size of the data disk.
                          type: string
                      type: object
                    type: array
                  defaultVersionNumber:
                    type: number
                  deploymentSetId:
                    description: The Launch Template ID.
                    type: string
                  description:
                    description: Description of instance launch template version 1.
                      It can be [2, 256] characters in length. It cannot start with
                      "http://" or "https://". The default value is null.
                    type: string
                  enableVmOsConfig:
                    type: boolean
                  hostName:
                    description: 'Instance host name.It cannot start or end with a
                      period (.) or a hyphen (-) and it cannot have two or more consecutive
                      periods (.) or hyphens (-).For Windows: The host name can be
                      [2, 15] characters in length. It can contain A-Z, a-z, numbers,
                      periods (.), and hyphens (-). It cannot only contain numbers.
                      For other operating systems: The host name can be [2, 64] characters
                      in length. It can be segments separated by periods (.). It can
                      contain A-Z, a-z, numbers, and hyphens (-).'
                    type: string
                  httpEndpoint:
                    type: string
                  httpPutResponseHopLimit:
                    type: number
                  httpTokens:
                    type: string
                  imageId:
                    description: Image ID.
                    type: string
                  imageOwnerAlias:
                    type: string
                  instanceChargeType:
                    description: 'Billing methods. Optional values:'
                    type: string
                  instanceName:
                    description: The name of the instance. The name is a string of
                      2 to 128 characters. It must begin with an English or a Chinese
                      character. It can contain A-Z, a-z, Chinese characters, numbers,
                      periods (.), colons (:), underscores (_), and hyphens (-).
                    type: string
                  instanceType:
                    description: Instance type. For more information, call resource_alicloud_instances
                      to obtain the latest instance type list.
                    type: string
                  internetChargeType:
                    description: 'Internet bandwidth billing method. Optional values:
                      PayByTraffic | PayByBandwidth.'
                    type: string
                  internetMaxBandwidthIn:
                    description: 'The maximum inbound bandwidth from the Internet
                      network, measured in Mbit/s. Value range: [1, 200].'
                    type: number
                  internetMaxBandwidthOut:
                    description: 'Maximum outbound bandwidth from the Internet, its
                      unit of measurement is Mbit/s. Value range: [0, 100].'
                    type: number
                  ioOptimized:
                    description: 'Whether it is an I/O-optimized instance or not.
                      Optional values:'
                    type: string
                  keyPairName:
                    description: The name of the key pair.
                    type: string
                  launchTemplateName:
                    description: Instance launch template name. Can contain [2, 128]
                      characters in length. It must start with an English letter or
                      Chinese, can contain numbers, periods (.), colons (:), underscores
                      (_), and hyphens (-). It cannot start with "http://" or "https://".
                    type: string
                  name:
                    description: Instance launch template name. Can contain [2, 128]
                      characters in length. It must start with an English letter or
                      Chinese, can contain numbers, periods (.), colons (:), underscores
                      (_), and hyphens (-). It cannot start with "http://" or "https://".
                    type: string
                  networkInterfaces:
                    description: The list of network interfaces created with instance.
                    items:
                      properties:
                        description:
                          description: Description of instance launch template version
                            1. It can be [2, 256] characters in length. It cannot
                            start with "http://" or "https://". The default value
                            is null.
                          type: string
                        name:
                          description: Instance launch template name. Can contain
                            [2, 128] characters in length. It must start with an English
                            letter or Chinese, can contain numbers, periods (.), colons
                            (:), underscores (_), and hyphens (-). It cannot start
                            with "http://" or "https://".
                          type: string
                        primaryIp:
                          description: The primary private IP address of the ENI.
                          type: string
                        securityGroupId:
                          description: The security group ID.
                          type: string
                        vswitchId:
                          description: When creating a VPC-Connected instance, you
                            must specify its VSwitch ID.
                          type: string
                      type: object
                    type: array
                  networkType:
                    description: 'Network type of the instance. Value options: classic
                      | vpc.'
                    type: string
                  passwordInherit:
                    type: boolean
                  period:
                    type: number
                  periodUnit:
                    type: string
                  privateIpAddress:
                    type: string
                  ramRoleName:
                    description: The RAM role name of the instance. You can use the
                      RAM API ListRoles to query instance RAM role names.
                    type: string
                  resourceGroupId:
                    description: The Launch Template ID.
                    type: string
                  securityEnhancementStrategy:
                    description: 'Whether or not to activate the security enhancement
                      feature and install network security software free of charge.
                      Optional values: Active | Deactive.'
                    type: string
                  securityGroupId:
                    description: The security group ID.
                    type: string
                  securityGroupIds:
                    items:
                      type: string
                    type: array
                  spotDuration:
                    type: string
                  spotPriceLimit:
                    description: Sets the maximum hourly instance price. Supports
                      up to three decimal places.
                    type: number
                  spotStrategy:
                    description: 'The spot strategy for a Pay-As-You-Go instance.
                      This parameter is valid and required only when InstanceChargeType
                      is set to PostPaid. Value range:'
                    type: string
                  systemDisk:
                    items:
                      properties:
                        category:
                          description: 'The category of the disk:'
                          type: string
                        deleteWithInstance:
                          description: Delete this data disk when the instance is
                            destroyed. It only works on cloud, cloud_efficiency, cloud_ssd
                            and cloud_essd disk. If the category of this data disk
                            was ephemeral_ssd, please don't set this param.
                          type: boolean
                        description:
                          description: Description of instance launch template version
                            1. It can be [2, 256] characters in length. It cannot
                            start with "http://" or "https://". The default value
                            is null.
                          type: string
                        encrypted:
                          description: Encrypted the data in this disk.
                          type: boolean
                        iops:
                          type: string
                        name:
                          description: Instance launch template name. Can contain
                            [2, 128] characters in length. It must start with an English
                            letter or Chinese, can contain numbers, periods (.), colons
                            (:), underscores (_), and hyphens (-). It cannot start
                            with "http://" or "https://".
                          type: string
                        performanceLevel:
                          type: string
                        size:
                          description: The size of the data disk.
                          type: number
                      type: object
                    type: array
                  systemDiskCategory:
                    description: 'The category of the system disk. System disk type.
                      Optional values:'
                    type: string
                  systemDiskDescription:
                    description: System disk description. It cannot begin with http://
                      or https://.
                    type: string
                  systemDiskName:
                    description: System disk name. The name is a string of 2 to 128
                      characters. It must begin with an English or a Chinese character.
                      It can contain A-Z, a-z, Chinese characters, numbers, periods
                      (.), colons (:), underscores (_), and hyphens (-).
                    type: string
                  systemDiskSize:
                    description: 'Size of the system disk, measured in GB. Value range:
                      [20, 500].'
                    type: number
                  tags:
                    additionalProperties:
                      type: string
                    description: A mapping of tags to assign to the resource.
                    type: object
                    x-kubernetes-map-type: granular
                  templateResourceGroupId:
                    description: The Launch Template ID.
                    type: string
                  templateTags:
                    additionalProperties:
                      type: string
                    description: A mapping of tags to assign to the resource.
                    type: object
                    x-kubernetes-map-type: granular
                  updateDefaultVersionNumber:
                    type: boolean
                  userData:
                    type: string
                  userdata:
                    description: User data of the instance, which is Base64-encoded.
                      Size of the raw data cannot exceed 16 KB.
                    type: string
                  versionDescription:
                    description: Description of instance launch template version 1.
                      It can be [2, 256] characters in length. It cannot start with
                      "http://" or "https://". The default value is null.
                    type: string
                  vpcId:
                    description: The Launch Template ID.
                    type: string
                  vswitchId:
                    description: When creating a VPC-Connected instance, you must
                      specify its VSwitch ID.
                    type: string
                  zoneId:
                    description: The zone ID of the instance.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  autoReleaseTime:
                    description: Instance auto release time. The time is presented
                      using the ISO8601 standard and in UTC time. The format is  YYYY-MM-DDTHH:MM:SSZ.
                    type: string
                  autoRenew:
                    type: boolean
                  autoRenewPeriod:
                    type: number
                  dataDisks:
                    description: The list of data disks created with instance.
                    items:
                      properties:
                        category:
                          description: 'The category of the disk:'
                          type: string
                        deleteWithInstance:
                          description: Delete this data disk when the instance is
                            destroyed. It only works on cloud, cloud_efficiency, cloud_ssd
                            and cloud_essd disk. If the category of this data disk
                            was ephemeral_ssd, please don't set this param.
                          type: boolean
                        description:
                          description: Description of instance launch template version
                            1. It can be [2, 256] characters in length. It cannot
                            start with "http://" or "https://". The default value
                            is null.
                          type: string
                        device:
                          type: string
                        encrypted:
                          description: Encrypted the data in this disk.
                          type: boolean
                        name:
                          description: Instance launch template name. Can contain
                            [2, 128] characters in length. It must start with an English
                            letter or Chinese, can contain numbers, periods (.), colons
                            (:), underscores (_), and hyphens (-). It cannot start
                            with "http://" or "https://".
                          type: string
                        performanceLevel:
                          type: string
                        size:
                          description: The size of the data disk.
                          type: number
                        snapshotId:
                          description: The snapshot ID used to initialize the data
                            disk. If the size specified by snapshot is greater that
                            the size of the disk, use the size specified by snapshot
                            as the size of the data disk.
                          type: string
                      type: object
                    type: array
                  defaultVersionNumber:
                    type: number
                  deploymentSetId:
                    description: The Launch Template ID.
                    type: string
                  description:
                    description: Description of instance launch template version 1.
                      It can be [2, 256] characters in length. It cannot start with
                      "http://" or "https://". The default value is null.
                    type: string
                  enableVmOsConfig:
                    type: boolean
                  hostName:
                    description: 'Instance host name.It cannot start or end with a
                      period (.) or a hyphen (-) and it cannot have two or more consecutive
                      periods (.) or hyphens (-).For Windows: The host name can be
                      [2, 15] characters in length. It can contain A-Z, a-z, numbers,
                      periods (.), and hyphens (-). It cannot only contain numbers.
                      For other operating systems: The host name can be [2, 64] characters
                      in length. It can be segments separated by periods (.). It can
                      contain A-Z, a-z, numbers, and hyphens (-).'
                    type: string
                  httpEndpoint:
                    type: string
                  httpPutResponseHopLimit:
                    type: number
                  httpTokens:
                    type: string
                  imageId:
                    description: Image ID.
                    type: string
                  imageOwnerAlias:
                    type: string
                  instanceChargeType:
                    description: 'Billing methods. Optional values:'
                    type: string
                  instanceName:
                    description: The name of the instance. The name is a string of
                      2 to 128 characters. It must begin with an English or a Chinese
                      character. It can contain A-Z, a-z, Chinese characters, numbers,
                      periods (.), colons (:), underscores (_), and hyphens (-).
                    type: string
                  instanceType:
                    description: Instance type. For more information, call resource_alicloud_instances
                      to obtain the latest instance type list.
                    type: string
                  internetChargeType:
                    description: 'Internet bandwidth billing method. Optional values:
                      PayByTraffic | PayByBandwidth.'
                    type: string
                  internetMaxBandwidthIn:
                    description: 'The maximum inbound bandwidth from the Internet
                      network, measured in Mbit/s. Value range: [1, 200].'
                    type: number
                  internetMaxBandwidthOut:
                    description: 'Maximum outbound bandwidth from the Internet, its
                      unit of measurement is Mbit/s. Value range: [0, 100].'
                    type: number
                  ioOptimized:
                    description: 'Whether it is an I/O-optimized instance or not.
                      Optional values:'
                    type: string
                  keyPairName:
                    description: The name of the key pair.
                    type: string
                  launchTemplateName:
                    description: Instance launch template name. Can contain [2, 128]
                      characters in length. It must start with an English letter or
                      Chinese, can contain numbers, periods (.), colons (:), underscores
                      (_), and hyphens (-). It cannot start with "http://" or "https://".
                    type: string
                  name:
                    description: Instance launch template name. Can contain [2, 128]
                      characters in length. It must start with an English letter or
                      Chinese, can contain numbers, periods (.), colons (:), underscores
                      (_), and hyphens (-). It cannot start with "http://" or "https://".
                    type: string
                  networkInterfaces:
                    description: The list of network interfaces created with instance.
                    items:
                      properties:
                        description:
                          description: Description of instance launch template version
                            1. It can be [2, 256] characters in length. It cannot
                            start with "http://" or "https://". The default value
                            is null.
                          type: string
                        name:
                          description: Instance launch template name. Can contain
                            [2, 128] characters in length. It must start with an English
                            letter or Chinese, can contain numbers, periods (.), colons
                            (:), underscores (_), and hyphens (-). It cannot start
                            with "http://" or "https://".
                          type: string
                        primaryIp:
                          description: The primary private IP address of the ENI.
                          type: string
                        securityGroupId:
                          description: The security group ID.
                          type: string
                        vswitchId:
                          description: When creating a VPC-Connected instance, you
                            must specify its VSwitch ID.
                          type: string
                      type: object
                    type: array
                  networkType:
                    description: 'Network type of the instance. Value options: classic
                      | vpc.'
                    type: string
                  passwordInherit:
                    type: boolean
                  period:
                    type: number
                  periodUnit:
                    type: string
                  privateIpAddress:
                    type: string
                  ramRoleName:
                    description: The RAM role name of the instance. You can use the
                      RAM API ListRoles to query instance RAM role names.
                    type: string
                  resourceGroupId:
                    description: The Launch Template ID.
                    type: string
                  securityEnhancementStrategy:
                    description: 'Whether or not to activate the security enhancement
                      feature and install network security software free of charge.
                      Optional values: Active | Deactive.'
                    type: string
                  securityGroupId:
                    description: The security group ID.
                    type: string
                  securityGroupIds:
                    items:
                      type: string
                    type: array
                  spotDuration:
                    type: string
                  spotPriceLimit:
                    description: Sets the maximum hourly instance price. Supports
                      up to three decimal places.
                    type: number
                  spotStrategy:
                    description: 'The spot strategy for a Pay-As-You-Go instance.
                      This parameter is valid and required only when InstanceChargeType
                      is set to PostPaid. Value range:'
                    type: string
                  systemDisk:
                    items:
                      properties:
                        category:
                          description: 'The category of the disk:'
                          type: string
                        deleteWithInstance:
                          description: Delete this data disk when the instance is
                            destroyed. It only works on cloud, cloud_efficiency, cloud_ssd
                            and cloud_essd disk. If the category of this data disk
                            was ephemeral_ssd, please don't set this param.
                          type: boolean
                        description:
                          description: Description of instance launch template version
                            1. It can be [2, 256] characters in length. It cannot
                            start with "http://" or "https://". The default value
                            is null.
                          type: string
                        encrypted:
                          description: Encrypted the data in this disk.
                          type: boolean
                        iops:
                          type: string
                        name:
                          description: Instance launch template name. Can contain
                            [2, 128] characters in length. It must start with an English
                            letter or Chinese, can contain numbers, periods (.), colons
                            (:), underscores (_), and hyphens (-). It cannot start
                            with "http://" or "https://".
                          type: string
                        performanceLevel:
                          type: string
                        size:
                          description: The size of the data disk.
                          type: number
                      type: object
                    type: array
                  systemDiskCategory:
                    description: 'The category of the system disk. System disk type.
                      Optional values:'
                    type: string
                  systemDiskDescription:
                    description: System disk description. It cannot begin with http://
                      or https://.
                    type: string
                  systemDiskName:
                    description: System disk name. The name is a string of 2 to 128
                      characters. It must begin with an English or a Chinese character.
                      It can contain A-Z, a-z, Chinese characters, numbers, periods
                      (.), colons (:), underscores (_), and hyphens (-).
                    type: string
                  systemDiskSize:
                    description: 'Size of the system disk, measured in GB. Value range:
                      [20, 500].'
                    type: number
                  tags:
                    additionalProperties:
                      type: string
                    description: A mapping of tags to assign to the resource.
                    type: object
                    x-kubernetes-map-type: granular
                  templateResourceGroupId:
                    description: The Launch Template ID.
                    type: string
                  templateTags:
                    additionalProperties:
                      type: string
                    description: A mapping of tags to assign to the resource.
                    type: object
                    x-kubernetes-map-type: granular
                  updateDefaultVersionNumber:
                    type: boolean
                  userData:
                    type: string
                  userdata:
                    description: User data of the instance, which is Base64-encoded.
                      Size of the raw data cannot exceed 16 KB.
                    type: string
                  versionDescription:
                    description: Description of instance launch template version 1.
                      It can be [2, 256] characters in length. It cannot start with
                      "http://" or "https://". The default value is null.
                    type: string
                  vpcId:
                    description: The Launch Template ID.
                    type: string
                  vswitchId:
                    description: When creating a VPC-Connected instance, you must
                      specify its VSwitch ID.
                    type: string
                  zoneId:
                    description: The zone ID of the instance.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: LaunchTemplateStatus defines the observed state of LaunchTemplate.
            properties:
              atProvider:
                properties:
                  autoReleaseTime:
                    description: Instance auto release time. The time is presented
                      using the ISO8601 standard and in UTC time. The format is  YYYY-MM-DDTHH:MM:SSZ.
                    type: string
                  autoRenew:
                    type: boolean
                  autoRenewPeriod:
                    type: number
                  dataDisks:
                    description: The list of data disks created with instance.
                    items:
                      properties:
                        category:
                          description: 'The category of the disk:'
                          type: string
                        deleteWithInstance:
                          description: Delete this data disk when the instance is
                            destroyed. It only works on cloud, cloud_efficiency, cloud_ssd
                            and cloud_essd disk. If the category of this data disk
                            was ephemeral_ssd, please don't set this param.
                          type: boolean
                        description:
                          description: Description of instance launch template version
                            1. It can be [2, 256] characters in length. It cannot
                            start with "http://" or "https://". The default value
                            is null.
                          type: string
                        device:
                          type: string
                        encrypted:
                          description: Encrypted the data in this disk.
                          type: boolean
                        name:
                          description: Instance launch template name. Can contain
                            [2, 128] characters in length. It must start with an English
                            letter or Chinese, can contain numbers, periods (.), colons
                            (:), underscores (_), and hyphens (-). It cannot start
                            with "http://" or "https://".
                          type: string
                        performanceLevel:
                          type: string
                        size:
                          description: The size of the data disk.
                          type: number
                        snapshotId:
                          description: The snapshot ID used to initialize the data
                            disk. If the size specified by snapshot is greater that
                            the size of the disk, use the size specified by snapshot
                            as the size of the data disk.
                          type: string
                      type: object
                    type: array
                  defaultVersionNumber:
                    type: number
                  deploymentSetId:
                    description: The Launch Template ID.
                    type: string
                  description:
                    description: Description of instance launch template version 1.
                      It can be [2, 256] characters in length. It cannot start with
                      "http://" or "https://". The default value is null.
                    type: string
                  enableVmOsConfig:
                    type: boolean
                  hostName:
                    description: 'Instance host name.It cannot start or end with a
                      period (.) or a hyphen (-) and it cannot have two or more consecutive
                      periods (.) or hyphens (-).For Windows: The host name can be
                      [2, 15] characters in length. It can contain A-Z, a-z, numbers,
                      periods (.), and hyphens (-). It cannot only contain numbers.
                      For other operating systems: The host name can be [2, 64] characters
                      in length. It can be segments separated by periods (.). It can
                      contain A-Z, a-z, numbers, and hyphens (-).'
                    type: string
                  httpEndpoint:
                    type: string
                  httpPutResponseHopLimit:
                    type: number
                  httpTokens:
                    type: string
                  id:
                    description: The Launch Template ID.
                    type: string
                  imageId:
                    description: Image ID.
                    type: string
                  imageOwnerAlias:
                    type: string
                  instanceChargeType:
                    description: 'Billing methods. Optional values:'
                    type: string
                  instanceName:
                    description: The name of the instance. The name is a string of
                      2 to 128 characters. It must begin with an English or a Chinese
                      character. It can contain A-Z, a-z, Chinese characters, numbers,
                      periods (.), colons (:), underscores (_), and hyphens (-).
                    type: string
                  instanceType:
                    description: Instance type. For more information, call resource_alicloud_instances
                      to obtain the latest instance type list.
                    type: string
                  internetChargeType:
                    description: 'Internet bandwidth billing method. Optional values:
                      PayByTraffic | PayByBandwidth.'
                    type: string
                  internetMaxBandwidthIn:
                    description: 'The maximum inbound bandwidth from the Internet
                      network, measured in Mbit/s. Value range: [1, 200].'
                    type: number
                  internetMaxBandwidthOut:
                    description: 'Maximum outbound bandwidth from the Internet, its
                      unit of measurement is Mbit/s. Value range: [0, 100].'
                    type: number
                  ioOptimized:
                    description: 'Whether it is an I/O-optimized instance or not.
                      Optional values:'
                    type: string
                  keyPairName:
                    description: The name of the key pair.
                    type: string
                  latestVersionNumber:
                    type: number
                  launchTemplateName:
                    description: Instance launch template name. Can contain [2, 128]
                      characters in length. It must start with an English letter or
                      Chinese, can contain numbers, periods (.), colons (:), underscores
                      (_), and hyphens (-). It cannot start with "http://" or "https://".
                    type: string
                  name:
                    description: Instance launch template name. Can contain [2, 128]
                      characters in length. It must start with an English letter or
                      Chinese, can contain numbers, periods (.), colons (:), underscores
                      (_), and hyphens (-). It cannot start with "http://" or "https://".
                    type: string
                  networkInterfaces:
                    description: The list of network interfaces created with instance.
                    items:
                      properties:
                        description:
                          description: Description of instance launch template version
                            1. It can be [2, 256] characters in length. It cannot
                            start with "http://" or "https://". The default value
                            is null.
                          type: string
                        name:
                          description: Instance launch template name. Can contain
                            [2, 128] characters in length. It must start with an English
                            letter or Chinese, can contain numbers, periods (.), colons
                            (:), underscores (_), and hyphens (-). It cannot start
                            with "http://" or "https://".
                          type: string
                        primaryIp:
                          description: The primary private IP address of the ENI.
                          type: string
                        securityGroupId:
                          description: The security group ID.
                          type: string
                        vswitchId:
                          description: When creating a VPC-Connected instance, you
                            must specify its VSwitch ID.
                          type: string
                      type: object
                    type: array
                  networkType:
                    description: 'Network type of the instance. Value options: classic
                      | vpc.'
                    type: string
                  passwordInherit:
                    type: boolean
                  period:
                    type: number
                  periodUnit:
                    type: string
                  privateIpAddress:
                    type: string
                  ramRoleName:
                    description: The RAM role name of the instance. You can use the
                      RAM API ListRoles to query instance RAM role names.
                    type: string
                  resourceGroupId:
                    description: The Launch Template ID.
                    type: string
                  securityEnhancementStrategy:
                    description: 'Whether or not to activate the security enhancement
                      feature and install network security software free of charge.
                      Optional values: Active | Deactive.'
                    type: string
                  securityGroupId:
                    description: The security group ID.
                    type: string
                  securityGroupIds:
                    items:
                      type: string
                    type: array
                  spotDuration:
                    type: string
                  spotPriceLimit:
                    description: Sets the maximum hourly instance price. Supports
                      up to three decimal places.
                    type: number
                  spotStrategy:
                    description: 'The spot strategy for a Pay-As-You-Go instance.
                      This parameter is valid and required only when InstanceChargeType
                      is set to PostPaid. Value range:'
                    type: string
                  systemDisk:
                    items:
                      properties:
                        category:
                          description: 'The category of the disk:'
                          type: string
                        deleteWithInstance:
                          description: Delete this data disk when the instance is
                            destroyed. It only works on cloud, cloud_efficiency, cloud_ssd
                            and cloud_essd disk. If the category of this data disk
                            was ephemeral_ssd, please don't set this param.
                          type: boolean
                        description:
                          description: Description of instance launch template version
                            1. It can be [2, 256] characters in length. It cannot
                            start with "http://" or "https://". The default value
                            is null.
                          type: string
                        encrypted:
                          description: Encrypted the data in this disk.
                          type: boolean
                        iops:
                          type: string
                        name:
                          description: Instance launch template name. Can contain
                            [2, 128] characters in length. It must start with an English
                            letter or Chinese, can contain numbers, periods (.), colons
                            (:), underscores (_), and hyphens (-). It cannot start
                            with "http://" or "https://".
                          type: string
                        performanceLevel:
                          type: string
                        size:
                          description: The size of the data disk.
                          type: number
                      type: object
                    type: array
                  systemDiskCategory:
                    description: 'The category of the system disk. System disk type.
                      Optional values:'
                    type: string
                  systemDiskDescription:
                    description: System disk description. It cannot begin with http://
                      or https://.
                    type: string
                  systemDiskName:
                    description: System disk name. The name is a string of 2 to 128
                      characters. It must begin with an English or a Chinese character.
                      It can contain A-Z, a-z, Chinese characters, numbers, periods
                      (.), colons (:), underscores (_), and hyphens (-).
                    type: string
                  systemDiskSize:
                    description: 'Size of the system disk, measured in GB. Value range:
                      [20, 500].'
                    type: number
                  tags:
                    additionalProperties:
                      type: string
                    description: A mapping of tags to assign to the resource.
                    type: object
                    x-kubernetes-map-type: granular
                  templateResourceGroupId:
                    description: The Launch Template ID.
                    type: string
                  templateTags:
                    additionalProperties:
                      type: string
                    description: A mapping of tags to assign to the resource.
                    type: object
                    x-kubernetes-map-type: granular
                  updateDefaultVersionNumber:
                    type: boolean
                  userData:
                    type: string
                  userdata:
                    description: User data of the instance, which is Base64-encoded.
                      Size of the raw data cannot exceed 16 KB.
                    type: string
                  versionDescription:
                    description: Description of instance launch template version 1.
                      It can be [2, 256] characters in length. It cannot start with
                      "http://" or "https://". The default value is null.
                    type: string
                  vpcId:
                    description: The Launch Template ID.
                    type: string
                  vswitchId:
                    description: When creating a VPC-Connected instance, you must
                      specify its VSwitch ID.
                    type: string
                  zoneId:
                    description: The zone ID of the instance.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
